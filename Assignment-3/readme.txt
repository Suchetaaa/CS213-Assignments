The problems are specified in A3.pdf

To run xyz.cpp, use the command 'g++ xyz.cpp' which will create an executable a.out in the same directory
Run the executable as ./a.out

To test your code, run the command (an example):
./a.out < test/p1_t1.txt